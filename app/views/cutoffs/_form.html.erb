<%= form_for(@cutoff) do |f| %>
  <% if @cutoff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cutoff.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% @cutoff.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label "Stream" %><br>
    <%= select("cutoff", "stream_id", Stream.all.collect {|c| [c.stream, c.id]}) %>

  </div>
  <div class="field">
    <%= f.label "Category" %><br>
    <%= select("cutoff", "category_id", Category.all.collect {|c| [c.category, c.id]}) %>
  </div>
  <div class="field">
    <%= f.label :cutoff_marks %><br>
    <%= f.number_field :cutoff_marks %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
