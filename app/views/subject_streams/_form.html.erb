<div class="body-unit">
  <%= form_for(@subject_stream) do |f| %>
    <% if @subject_stream.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@subject_stream.errors.count, "error") %> prohibited this subject_stream from being saved:</h2>

        <ul>
        <% @subject_stream.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.label :subject_stream %><br>
      <%= f.text_field :subject_stream %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
</div>
